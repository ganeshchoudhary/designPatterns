package com.bytecoder.DesignPatterns.structural.pipe_filters.example1;

import java.util.function.Function;

public class CustomFunctial implements Function {
    @Override
    public Object apply(Object o) {
        return null;

//        list of event trigger services
//        list of read validate file and read as list<Lines> -> create objects -> validate objects and pass validate objects -> create
//         spring batch -> chunks -> reader -> transaforms -> process -> write
    }
}
